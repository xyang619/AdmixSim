CMAKE_MINIMUM_REQUIRED(VERSION 3.0)

PROJECT(AdmixSim VERSION 1.0)

SET(CMAKE_CXX_STANDARD 11)

SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/bin)

FILE(GLOB SRCS src/*.cpp src/*.hpp)

ADD_EXECUTABLE(AdmixSim ${SRCS})
